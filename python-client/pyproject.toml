[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "poi-client"
version = "0.1.0"
description = "Python SDK for Decentralized Proof-of-Contact Network"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27", "pydantic>=2.0"]
